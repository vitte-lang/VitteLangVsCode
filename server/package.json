{
  "name": "vitte-server",
  "private": true,
  "version": "0.1.0",
  "description": "Language Server Protocol (LSP) server for the Vitte/Vitl language",
  "author": "VitteStudio",
  "license": "MIT",
  "main": "./out/server.js",
  "files": [
    "out/**/*",
    "src/**/*",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -b",
    "watch": "tsc -b -w",
    "clean": "tsc -b --clean",
    "lint": "tsc --noEmit",
    "test": "echo \"No tests defined\" && exit 0"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "eslint": "9.35.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.11"
  }
}
